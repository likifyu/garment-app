# 服装设计管理系统

一个基于 Web 的服装设计与样板管理系统，帮助设计师管理设计稿、样板文件和历史版本。

## 📋 功能特性

### 设计稿管理
- ✅ 上传和管理设计稿图片
- ✅ 设计稿分类（春夏秋冬、运动、休闲等）
- ✅ 设计师信息记录
- ✅ 设计状态管理（草稿/已发布/已归档）
- ✅ 设计稿历史版本追踪

### 样板管理
- ✅ 样板文件上传（PDF、DWG、DXF 等）
- ✅ 尺码管理（S/M/L/XL/XXL）
- ✅ 样板分类（上装/下装/套装/配饰）
- ✅ 关联设计稿
- ✅ 样板说明记录

### 数据统计
- ✅ 设计稿数量统计
- ✅ 样板数量统计
- ✅ 版本历史统计
- ✅ 最近上传展示

## 🛠 技术栈

### 后端
- **Node.js** - JavaScript 运行时
- **Express** - Web 应用框架
- **Prisma** - ORM 数据库工具
- **SQLite** - 轻量级数据库
- **Multer** - 文件上传处理

### 前端
- **Vue 3** - 渐进式 JavaScript 框架
- **Vite** - 新一代前端构建工具
- **Element Plus** - Vue 3 UI 组件库
- **Pinia** - Vue 状态管理
- **Vue Router** - 路由管理
- **Axios** - HTTP 客户端

## 📦 项目结构

```
garment-app/
├── backend/                 # 后端服务
│   ├── src/
│   │   ├── config/         # 配置文件
│   │   ├── controllers/    # 控制器
│   │   ├── models/         # Prisma 模型
│   │   ├── routes/         # 路由定义
│   │   ├── services/       # 业务逻辑
│   │   ├── middlewares/    # 中间件
│   │   └── app.js          # 应用入口
│   ├── uploads/            # 文件上传目录
│   ├── database/           # 数据库文件
│   └── package.json
│
├── frontend/                # 前端应用
│   ├── src/
│   │   ├── api/            # API 调用
│   │   ├── components/     # Vue 组件
│   │   ├── views/          # 页面视图
│   │   ├── router/         # 路由配置
│   │   ├── stores/         # 状态管理
│   │   ├── utils/          # 工具函数
│   │   ├── App.vue
│   │   └── main.js
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
│
└── README.md
```

## 🚀 快速开始

### 环境要求
- Node.js >= 16.x
- npm >= 8.x

### 安装步骤

#### 1. 克隆项目
```bash
cd /f/Dev/garment-app
```

#### 2. 安装后端依赖
```bash
cd backend
npm install
```

#### 3. 配置数据库
```bash
# 生成 Prisma Client
npm run prisma:generate

# 创建数据库表
npm run prisma:push
```

#### 4. 启动后端服务
```bash
npm start
# 或使用开发模式（需要安装 nodemon）
npm run dev
```

后端服务将运行在 `http://localhost:3000`

#### 5. 安装前端依赖
```bash
cd ../frontend
npm install
```

#### 6. 启动前端服务
```bash
npm run dev
```

前端应用将运行在 `http://localhost:5173`

## 📡 API 接口

### 设计稿 API

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/designs` | 创建设计稿 |
| GET | `/api/designs` | 获取设计稿列表 |
| GET | `/api/designs/:id` | 获取设计稿详情 |
| PUT | `/api/designs/:id` | 更新设计稿 |
| DELETE | `/api/designs/:id` | 删除设计稿 |
| POST | `/api/designs/:id/versions` | 创建设计稿版本 |
| GET | `/api/designs/:id/versions` | 获取版本列表 |

### 样板 API

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/patterns` | 创建样板 |
| GET | `/api/patterns` | 获取样板列表 |
| GET | `/api/patterns/:id` | 获取样板详情 |
| PUT | `/api/patterns/:id` | 更新样板 |
| DELETE | `/api/patterns/:id` | 删除样板 |

### 文件上传 API

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/upload/design` | 上传设计稿图片 |
| POST | `/api/upload/pattern` | 上传样板文件 |

## 🗄 数据库设计

### 设计稿表 (designs)
- `id` - 主键
- `title` - 设计标题
- `description` - 设计说明
- `imageUrl` - 图片路径
- `category` - 分类
- `season` - 季节
- `designer` - 设计师
- `status` - 状态
- `createdAt` - 创建时间
- `updatedAt` - 更新时间

### 样板表 (patterns)
- `id` - 主键
- `name` - 样板名称
- `size` - 尺码
- `category` - 分类
- `fileUrl` - 文件路径
- `description` - 样板说明
- `designId` - 关联设计稿ID
- `createdAt` - 创建时间
- `updatedAt` - 更新时间

### 设计版本表 (design_versions)
- `id` - 主键
- `designId` - 关联设计稿ID
- `version` - 版本号
- `title` - 版本标题
- `description` - 变更说明
- `imageUrl` - 版本图片
- `createdAt` - 创建时间

## 🔧 开发指南

### 添加新功能

1. **后端**：在 `backend/src/` 下添加相应的控制器、服务和路由
2. **前端**：在 `frontend/src/` 下添加相应的视图、组件和 API 调用

### 修改数据库

1. 编辑 `backend/src/models/schema.prisma`
2. 运行 `npm run prisma:push` 更新数据库

## 📝 许可证

MIT License

## 👥 作者

服装设计管理系统开发团队

---

**祝您使用愉快！** 🎉
